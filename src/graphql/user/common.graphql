type User {
  id: ID!
  email: String!
}

type UserResult {
  success: Boolean!
  path: String
  message: String
  result: User
}

type Query {
  getUser: UserResult!
  
}
type Mutation {
  login(email: String!, password: String!): UserResult!
  logout: Boolean!
  register(email: String!, password: String!): UserResult!
}