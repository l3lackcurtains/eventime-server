type User {
  id: ID!
  email: String!
}

type Result {
  success: Boolean!
  path: String
  message: String
  data: User
}

type Query {
  getUser: Result!
}

type Mutation {
  login(email: String!, password: String!): Result!
  register(email: String!, password: String!): Result!
}