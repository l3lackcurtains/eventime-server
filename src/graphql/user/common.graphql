type User {
  id: ID!
  email: String!
}

type UserResult {
  success: Boolean!
  path: String
  message: String
  data: User
}

type Query {
  getUser: UserResult!
}

type Mutation {
  login(email: String!, password: String!): UserResult!
  register(email: String!, password: String!): UserResult!
}